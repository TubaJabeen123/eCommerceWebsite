export const metadata = {
  title: 'Byte and Board Solutions',
  description: 'Byte and Board Solutions',
}

export default function RootLayout({ children }) {
  return ( 
      <body>{children}</body> 
  )
}
